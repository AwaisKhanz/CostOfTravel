{
  "name": "cost-of-travel-monorepo",
  "version": "1.0.0",
  "description": "Root configuration for deploying Cost of Travel on a single Render instance",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
